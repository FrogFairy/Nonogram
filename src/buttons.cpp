#include "buttons.h"

void Game_button::attach(Window &win)
{
//     pw = new Fl_Button{loc.x, loc.y, width, height, label.c_str()};
//     pw->callback(reinterpret_cast<Fl_Callback *>(do_it), this); // pass this widget
//     own = &win;
}

void Game_button::change_button()
{
    
}

void Game_button::change_color()
{
    // {
    //     pw->color(Graph_lib::Color::green);
    // }
}
